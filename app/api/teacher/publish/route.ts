import { hasTeacherSession } from '../../teacher-session';
import { json,sameOrigin,supabase } from '../grading-store';
export const runtime='nodejs';
export async function POST(request:Request){if(!hasTeacherSession(request))return json({error:'교사 로그인이 필요합니다.'},401);if(!sameOrigin(request))return json({error:'이 웹사이트에서 공개해 주세요.'},403);const db=supabase();if(!db)return json({error:'Supabase 연결 설정이 필요합니다.'},503);try{const {classroom}=await request.json() as {classroom?:unknown};if(typeof classroom!=='string'||!/^\d$/.test(classroom))return json({error:'학급을 선택해 주세요.'},400);const r=await fetch(`${db.url}/rest/v1/grading_results?select=submission_id,items,total_score,updated_at&classroom=like._${classroom}__`,{headers:db.headers,cache:'no-store'});if(!r.ok)return json({error:'채점 결과를 불러오지 못했습니다.'},502);const grades=await r.json() as {submission_id:string;items:unknown;total_score:number;updated_at:string}[];if(!grades.length)return json({error:'공개할 채점 결과가 없습니다.'},400);const results=Object.fromEntries(grades.map(g=>[g.submission_id,{items:g.items,total:g.total_score,gradedAt:g.updated_at}]));const publishedAt=new Date().toISOString();const save=await fetch(`${db.url}/rest/v1/class_score_publications?on_conflict=classroom`,{method:'POST',headers:{...db.headers,Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({classroom,results,published_at:publishedAt})});if(!save.ok)return json({error:'학급 점수를 공개하지 못했습니다.'},502);return json({published:true,count:grades.length,publishedAt});}catch{return json({error:'점수 공개 요청을 확인해 주세요.'},400)}}
